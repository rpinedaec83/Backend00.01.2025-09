doctype html
html
    head
        meta(charset="utf-8")
        title Hackaton 07 - API Gateway
        link(rel="stylesheet", href="/styles.css")
    body
        h1 Hackaton 07 - API Gateway
        p Selecciona qué quieres consultar:

        h2 GitHub
        form(action="/github" method="get")
            label(for="username") Usuario de GitHub:
            input(type="text" id="username" name="username" placeholder="RonyChang" required)
            button(type="submit") Buscar

        h2 Clima
        form(action="/clima" method="get")
            label(for="ciudad") Ciudad:
            input(type="text" id="clima" name="ciudad" placeholder="Lima" required)
            button(type="submit") Ver clima

        h2 Tipo de cambio desde USD
        form(action="/cambio" method="get")
            label(for="to") Moneda destino:
            input(type="text" id="to" name="to" placeholder="PEN" maxlength="3")
            label(for="amount") Monto en USD:
            input(type="number" id="amount" name="amount" value="1" min="1" step="0.01")
            button(type="submit") Convertir

        h2 Pokemones
        h3 Lista de Pokemones
        form(action="/pokemones" method="get")
            label(for="limit") Cantidad (o escribe "todos"):
            input(type="text" id="limit" name="limit" placeholder="todos")
            button(type="submit") Ver lista
        h3 Poderes de Pokemon
        form(action="/pokemon" method="get")
            label(for="name") Elige un Pokémon:
            input(type="text" id="name" name="name" placeholder="pikachu" required)
            button(type="submit") Buscar

        h2 Marte
            a(href="/mars") Ver datos de Marte