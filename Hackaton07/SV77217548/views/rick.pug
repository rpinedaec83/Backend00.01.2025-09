doctype html
html
  head
    meta(charset="utf-8")
    title Rick and Morty - Lista de personajes
    link(rel="stylesheet", href="/styles.css")
  body
    h1 Lista de personajes - Rick and Morty

    p
      a(href="/") ← Volver al inicio

    form(action="/rick" method="get")
      label(for="limit") Cantidad (o escribe "todos"):
      input(type="text" id="limit" name="limit" placeholder="todos")
      button(type="submit") Ver lista

    if error
      p.error= error
    else if characters && characters.length
      ul
        each c in characters
          li
            a(href=`/rick/personaje?id=${c.id}`)= c.name
    else
      p No se encontraron personajes.

    hr
    details
      summary Ver JSON completo (info + characters)
      pre
        = JSON.stringify({ info, characters }, null, 2)
