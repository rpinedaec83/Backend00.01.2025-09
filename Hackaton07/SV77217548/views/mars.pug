doctype html
html
  head
    meta(charset="utf-8")
    title Datos de Marte
    link(rel="stylesheet", href="/styles.css")
  body
    a(href="/") ← Volver
    h1 Datos de Marte

    if error
      p(style="color:red")= error
    else if data
      // Para empezar, mostramos el JSON bruto
      pre= JSON.stringify(data, null, 2)
    else
      p No hay datos disponibles.
